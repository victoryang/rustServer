sudo: required
dist: trusty

language: go

go:
  - "1.10.x"

install:
  - chmod +x ci-install.sh && sh -C ci-install.sh

script:
  - make

deploy:
  provider: releases
  api_key: ${GH_TOKEN}
  file: rustServer
  skip_cleanup: true
  on:
    tags: true